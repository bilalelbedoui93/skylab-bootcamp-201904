/**
 * this function evaluates the value and returns true if it's an Array
 * @param {valuue} array The array to iterate.
 * 
 * @returns {boolean} True if all the value is an Array
 * 
 */

function isArray(value){
    return value instanceof Array;  
}